package com.softllc.location

import kotlinx.coroutines.flow.Flow

class GetLocationUseCase (
    //private val locationService: LocationService = getLocationService()
) {
    //operator fun invoke(): Flow<LocationLatLng?> = locationService.requestLocationUpdates()
}